rootProject.name = "javaGameEngine"  // Replace with your project name

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
    plugins {
        kotlin("jvm") version "1.9.20"
    }
}